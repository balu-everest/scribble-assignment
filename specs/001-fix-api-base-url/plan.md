# Implementation Plan: Fix API Base URL Bug

**Branch**: `001-fix-api-base-url` | **Date**: 2026-06-03 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `specs/001-fix-api-base-url/spec.md`

## Summary

Fix the default `API_BASE_URL` in `frontend/src/services/api.ts` from `"http://localhost:3001/bug"` to `"http://localhost:3001"` so the frontend can communicate with the backend without requiring a manual `VITE_API_URL` environment variable. Also tighten the associated tests to use exact URL assertions.

## Technical Context

**Language/Version**: TypeScript, React 18, Vite

**Primary Dependencies**: Node.js, Express (backend), React + Vite (frontend)

**Storage**: N/A

**Testing**: Vitest (frontend)

**Target Platform**: Web browser

**Project Type**: Web application (frontend + backend)

**Performance Goals**: N/A

**Constraints**: Must preserve `VITE_API_URL` env var override behavior

**Scale/Scope**: Single-line fix with test updates

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| In-Memory State Only | ✓ PASS | No state changes |
| Polling Synchronization | ✓ PASS | No sync changes |
| Fail-Fast Validation | ✓ PASS | Not user input |
| Viewer-Specific Security | ✓ PASS | No data exposure |
| Read-Before-Write | ✓ PASS | `/bug` only exists in `api.ts:22` |
| Spec-First Alignment | ✓ PASS | Spec created |
| Zero-Regression Rule | ✓ PASS | `VITE_API_URL` override preserved |
| Verification Gate | ✓ PASS | TS compilation + health check |

## Project Structure

### Documentation (this feature)

```text
specs/001-fix-api-base-url/
├── plan.md              # This file
└── spec.md              # Feature specification
```

### Source Code (affected files)

```text
frontend/
└── src/
    └── services/
        ├── api.ts           # Single-line change
        └── api.test.ts      # Tighten URL assertions
```

**Structure Decision**: Web application — the fix touches only the frontend API client layer.

## Complexity Tracking

*No constitution violations — Complexity Tracking section not needed.*

## Execution Phases

### Phase 0: Research (Complete)

- `/bug` only appears in `frontend/src/services/api.ts:22` — no other file references
- Backend runs on port 3001 by default (`backend/src/server.ts:3`)
- Tests use `expect.stringContaining()` which passes even with the broken URL

### Phase 1: Design (Not needed)

No data model changes, no new contracts. This is a trivial config fix.

### Phase 2: Tasks (Generated by `/speckit.tasks`)

1. Correct `API_BASE_URL` default in `frontend/src/services/api.ts:22`
2. Update tests in `frontend/src/services/api.test.ts` to use exact URL assertions
3. Verify TypeScript compilation and backend health check pass
